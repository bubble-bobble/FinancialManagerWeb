@model AccountTypeViewModel

@{
    ViewData["Title"] = "Delete account type";
}

<div class="d-flex justify-content-center align-items-center">
    <div class="alert alert-danger" role="alert">
        <form asp-controller="AccountTypes" asp-action="ConfirmDelete">
            <input type="hidden" asp-for="Id" />
            <h4 class="alert-heading">Delete alert!</h4>
            <p>Are you sure you want to delete <strong>@Model.Name</strong> account type?</p>
            <hr>
            <button class="btn btn-danger">Delete</button>
            <a asp-controller="AccountTypes" asp-action="Index" class="btn btn-secondary">
                Cancel
            </a>
        </form>
    </div>
</div>
